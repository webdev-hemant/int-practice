import React from 'react';
import styles from './hero.module.scss';

const HeroSection: React.FC = () => {
  return <div className={styles.heroWrapper}>HeroSection</div>;
};

export default HeroSection;
